{
  "name": "explore-plan",
  "description": "ExplorePlan Project created with Angular",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "ng": "ng",
    "start": "ng serve -o",
    "local": "ng serve -c local -o",
    "build": "ng build --prod",
    "postbuild": "node deploy/postbuild.js",
    "test": "ng test --code-coverage",
    "test-jasmine": "jasmine",
    "lint": "ng lint",
    "lint-fix": "ng lint explore-plan --fix && ng lint explore-plan-e2e --fix",
    "e2e": "ng e2e",
    "hmr": "ng serve --configuration hmr"
  },
  "engines": {
    "node": ">=8.9"
  },
  "private": true,
  "dependencies": {
    "buffer-from": "^1.1.1",
    "node-os-utils": "^1.0.7",
    "rxjs": "^6.1.0",
    "taggle": "^1.14.0",
    "tslib": "^1.9.0",
    "zone.js": "^0.8.26"
  },
  "devDependencies": {
    "ts-node": "~4.1.0",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  }
}